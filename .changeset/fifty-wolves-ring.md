---
"bundled-eslint-config": minor
---

Breaking change: Remove all plugins/rules already covered by oxlint. This config now only covers vue/astro templates and package.json. For the rest, use oxlint.
